name: Stage QA-approved image for production

on:
  workflow_dispatch:
    inputs:
      target_slot:
        description: Inactive production label to receive the QA-approved image
        required: true
        default: green
        type: choice
        options:
          - green
          - blue
      expected_sha:
        description: Full Git SHA already deployed and verified in isolated QA
        required: true
        type: string

permissions:
  contents: read
  id-token: write

concurrency:
  group: azure-staging
  cancel-in-progress: false

jobs:
  validate-build-deploy:
    runs-on: ubuntu-latest
    environment: azure-staging
    env:
      TARGET_SLOT: ${{ inputs.target_slot }}
      EXPECTED_SHA: ${{ inputs.expected_sha }}
      TARGET_URL: ${{ inputs.target_slot == 'blue' && vars.AZURE_BLUE_URL || vars.AZURE_GREEN_URL }}
      QA_URL: ${{ vars.AZURE_QA_URL }}
      PUBLIC_SITE_URL: https://filosage.com
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - name: Sign in to Azure with GitHub OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Verify the exact SHA in isolated QA
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! "$EXPECTED_SHA" =~ ^[a-fA-F0-9]{40}$ ]]; then
            echo "expected_sha must be a full 40-character Git SHA."
            exit 1
          fi
          if [[ "$EXPECTED_SHA" != "$GITHUB_SHA" ]]; then
            echo "Run this workflow from the exact branch or commit selected in QA."
            exit 1
          fi
          npm run check:production -- "$QA_URL" "$EXPECTED_SHA" "$QA_URL"

      - name: Deploy the exact image to Azure Container Apps
        id: deploy_revision
        shell: bash
        run: |
          set -euo pipefail
          DIGEST="$(az acr repository show \
            --name "${{ vars.AZURE_ACR_NAME }}" \
            --image "filosage:${EXPECTED_SHA}" \
            --query digest \
            --output tsv)"
          if [[ ! "$DIGEST" =~ ^sha256:[a-fA-F0-9]{64}$ ]]; then
            echo "The QA-approved image digest could not be resolved."
            exit 1
          fi
          IMAGE="${{ vars.AZURE_ACR_NAME }}.azurecr.io/filosage@${DIGEST}"
          ACTIVE_WEIGHT="$(az containerapp show \
            --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}" \
            --name "${{ vars.AZURE_CONTAINER_APP_NAME }}" \
            --query "properties.configuration.ingress.traffic[?label=='${TARGET_SLOT}'].weight | [0]" \
            --output tsv)"
          if [[ "${ACTIVE_WEIGHT:-0}" != "0" ]]; then
            echo "Refusing to replace ${TARGET_SLOT}; it currently carries ${ACTIVE_WEIGHT}% traffic."
            exit 1
          fi
          az containerapp revision set-mode \
            --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}" \
            --name "${{ vars.AZURE_CONTAINER_APP_NAME }}" \
            --mode multiple
          REVISION_SUFFIX="${TARGET_SLOT}-${EXPECTED_SHA:0:8}-${GITHUB_RUN_ATTEMPT}"
          az containerapp update \
            --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}" \
            --name "${{ vars.AZURE_CONTAINER_APP_NAME }}" \
            --image "$IMAGE" \
            --revision-suffix "$REVISION_SUFFIX" \
            --remove-env-vars \
              "NEXT_PUBLIC_ENTRA_CLIENT_ID" \
              "NEXT_PUBLIC_ENTRA_AUTHORITY" \
              "NEXT_PUBLIC_ENTRA_TENANT_ID" \
              "NEXT_PUBLIC_ENTRA_API_SCOPE" \
              "NEXT_PUBLIC_ENTRA_REDIRECT_URI" \
              "ENTRA_AUDIENCE" \
              "ENTRA_ISSUER" \
              "ENTRA_JWKS_URI" \
              "ENTRA_DIRECTORY_TENANT_ID" \
              "ENTRA_DIRECTORY_CLIENT_ID" \
              "ENTRA_DIRECTORY_CLIENT_SECRET" \
            --set-env-vars \
              "SITE_VERSION=${EXPECTED_SHA}" \
              "NEXT_PUBLIC_SITE_URL=${PUBLIC_SITE_URL}" \
              "DEPLOYMENT_ENVIRONMENT=production" \
              "OPERATIONS_ENVIRONMENT=azure-staging" \
              "AZURE_EASY_AUTH_ENABLED=true" \
              "BILLING_ENABLED=false" \
              "DEPLOYMENT_SLOT=${TARGET_SLOT}"
          REVISION="$(az containerapp show \
            --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}" \
            --name "${{ vars.AZURE_CONTAINER_APP_NAME }}" \
            --query properties.latestRevisionName \
            --output tsv)"
          if [[ -z "$REVISION" ]]; then
            echo "The deployed revision name could not be resolved."
            exit 1
          fi
          echo "revision=$REVISION" >> "$GITHUB_OUTPUT"

      - name: Verify the deployed release safety policy
        shell: bash
        run: |
          set -euo pipefail
          RESOURCE_GROUP="${{ vars.AZURE_RESOURCE_GROUP }}"
          APP_NAME="${{ vars.AZURE_CONTAINER_APP_NAME }}"
          REVISION_NAME="${{ steps.deploy_revision.outputs.revision }}"
          BILLING_STATE="$(az containerapp revision show --resource-group "$RESOURCE_GROUP" --name "$APP_NAME" --revision "$REVISION_NAME" --query "properties.template.containers[0].env[?name=='BILLING_ENABLED'].value | [0]" --output tsv)"
          EASY_AUTH_STATE="$(az containerapp revision show --resource-group "$RESOURCE_GROUP" --name "$APP_NAME" --revision "$REVISION_NAME" --query "properties.template.containers[0].env[?name=='AZURE_EASY_AUTH_ENABLED'].value | [0]" --output tsv)"
          SUBSCRIPTION_ID="$(az account show --query id --output tsv)"
          AUTH_URL="https://management.azure.com/subscriptions/${SUBSCRIPTION_ID}/resourceGroups/${RESOURCE_GROUP}/providers/Microsoft.App/containerApps/${APP_NAME}/authConfigs/current?api-version=2025-01-01"
          AUTH_ENABLED="$(az rest --method get --url "$AUTH_URL" --query properties.platform.enabled --output tsv)"
          GOOGLE_ENABLED="$(az rest --method get --url "$AUTH_URL" --query properties.identityProviders.google.enabled --output tsv)"
          if [[ "$BILLING_STATE" != "false" || "$EASY_AUTH_STATE" != "true" || "$AUTH_ENABLED" != "true" || "$GOOGLE_ENABLED" != "true" ]]; then
            echo "Release safety policy failed: billing must be closed and Google Easy Auth must be active."
            exit 1
          fi

      - name: Assign verified revision label
        shell: bash
        run: |
          set -euo pipefail
          az containerapp revision label add \
            --resource-group "${{ vars.AZURE_RESOURCE_GROUP }}" \
            --name "${{ vars.AZURE_CONTAINER_APP_NAME }}" \
            --label "$TARGET_SLOT" \
            --revision "${{ steps.deploy_revision.outputs.revision }}" \
            --yes

      - name: Verify the labeled QA candidate
        run: npm run check:production -- "${TARGET_URL}" "${EXPECTED_SHA}" "${PUBLIC_SITE_URL}"
