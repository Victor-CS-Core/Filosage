rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Legacy Firebase source access is server-only. The Azure-hosted browser
    // application never reads or writes Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
