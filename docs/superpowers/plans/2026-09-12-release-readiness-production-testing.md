# Filosage release readiness and production testing implementation record

Approved by Victor in the current task on September 12, 2026. Goal: release consolidated main for free-account production testing, audit and fix release friction, optimize from measurements, retire separate QA after successful blue/green acceptance, and assess React/.NET feasibility after release. Coordinator checklist: `docs/AGENT_PROGRESS.md` R1–R9.

## Binding defaults

Preserve design, URLs/API contracts, capability manifest and observed authentication mode. Anonymous access stops at public course outlines. Registration/authorization gates lesson participation. New checkout stays closed; existing subscriber obligations must be verified. Keep current scaling initially and report measured warm-replica benefit/cost. No .NET implementation, broad redesign or paid activation. Source evidence, CI, deployment and production acceptance are separate.

## Execution packages and acceptance

1. UI and application performance: baseline production build; audit landing/catalog/outline/auth/first lesson/saved progress/profile/pricing/support/enabled owner tools, recovery states and keyboard/accessibility on desktop/mobile/light/dark/Chromium/WebKit. Include actual non-owner hosted learner and hosted appearance checks. Record severity, screenshots and measured page/JS/API cold/warm behavior; fix blockers and clear friction. Collect system CPU/memory/restarts, database query/connections and model latency/cost; preserve current defaults pending evidence.
2. Release prerequisites: verify exact Azure topology and live configuration, GitHub reviewer/deployment controls, backup observation inputs, runtime minimum-access DB/vault boundaries and real allow/deny probes. Confirm recovery evidence and schema/account/publication/accounting overlap/in-flight compatibility. Build a verified compatible predecessor if current production cannot satisfy rollback requirements; never weaken gates.
3. Freeze and release: integrate independently reviewed fixes, commit/push checkpoints, pass lint/types/build/contracts/PostgreSQL/billing/security and full browser regression against frozen SHA. Use existing staging/review/promotion workflows, immutable image digest and canonical production origin. Stage at zero public traffic and prove unchanged public routing; verify actual label-origin auth/session/learner/privacy/flags/closed-checkout and operations with designated test accounts. Collect existing subscriber evidence.
4. Promote and observe: switch exact verified revision weights 100/0 without rebuilding and honor required reviews. Check apex/www, version/digest/health and real sign-in/lesson/progress/monitoring; observe 30 minutes with bounded reads. Roll back only to verified compatible predecessor for critical-journey failure, identity/version mismatch or two consecutive unhealthy reads. Retain predecessor through QA retirement.
5. QA retirement: inventory exact site/domain/certificate/DNS, app/revisions, DB/roles, blobs, MI/grants/secrets, auth app/callback consumers, payment endpoints and GitHub config. Preserve required QA content/recovery records and configured retention/holds. After production/candidate acceptance, remove integrations and entrypoints, then compute, access, and exclusive data/identity in dependency order. Protect every shared resource/production consumer; verify no dangling references and recheck prod; update runbooks. No permanent extra QA website; isolated ephemeral destructive/payment test fixtures remain permitted.
6. Architecture report after release: compare Next React+TypeScript BFF, incremental .NET behind same origin, React SPA+ASP.NET Core single deployed app. Map 68 handlers (recount), auth/authorization, transactions, billing webhooks, AI, rendering/SEO/security; reusable UI, rewrites, contract tests, migration sequence, effort and costs. Deliver recommendation, not platform migration.

Every package includes documented evidence and unresolved blockers. Preserve unfinished historical hosted checks in the coordinator record. Final completion requires all R1–R9 requested outcomes or an honest account of exact external blockers, with fresh readbacks and no unattended indefinite watcher.
