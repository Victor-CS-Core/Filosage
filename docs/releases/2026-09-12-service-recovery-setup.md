# Isolated service recovery setup

Victor requested executing the next fix and will personally perform non-owner sign-in. Preserve the full release checklist. This setup reuses source `7c48bc02ff6623a81fee382b194864f1d04b76c0` and image `sha256:95cc9529e6abcfdef5918f39996cfed55703e5b29b82d75304a69765c844b344`. No production or QA traffic, authentication, data or runtime binding changes.

Use subscription `bfc8f890-2681-43dc-8eac-51644341ae12`, group `filosage-staging-central-rg`, existing Central US environment `filosagestg-environment`, existing private PostgreSQL subnet/DNS. Exact new owned targets: server `filosage-recovery-20260912-b`; app `filosage-recovery-app-20260912`; managed identity `filosage-recovery-identity-20260912-b`; private Blob container `recovery-20260912-assets-b`. Prove absence and record operation ownership before each creation. Do not overwrite existing resources after uncertainty.

Refresh the source inspection immediately before execution; the retained snapshot is not a long-lived restore point. Restore the original private PostgreSQL server to that fresh inspected postgres transaction timestamp, with public access disabled, unchanged B1ms/32GiB, and no scheduled jobs. Fresh source inspection at22:48UTC found522documents/13courses, compatible schema, and no installed pg_cron in either database. Copy original asset keys into the separate private container, verify bytes/metadata/headers and retain private ownership information. Historical versions, committed marker loss and database-to-asset consistency remain separate checks.

Reuse the prior bounded recovery allowance: operational spend allowance USD1, maximum intended resource lifetime two hours from restore request, validation deadline60minutes. The database-only duration is not service RTO. Include actual app/account validation before claiming the approved15minute RPO/60minute RTO. If validation cannot finish, preserve failed evidence and clean up exact owned resources; do not leave an unbounded environment.

Create only `filosage_recovery_runtime` on the clone, with a new private password, connection limit3, table DML and no ownership/DDL/elevated membership or QA access. The reviewed helper pins the clone hostname before any connection and verifies the normal eight-column startup schema plus rolled-back DML. Production credentials remain inside the existing private transport; no production role or password changes.

The new identity receives only registry pull and Blob reader on the exact isolated container. App-local secrets are the clone-only DSN, a fresh activity receipt key, the existing identity-link key needed to resolve restored account mappings, and the existing Google OAuth client secret for managed authentication. Transport private values without terminal output or durable CLI logs; never place them in Git. Do not attach production or QA identities, database credentials, live AI/payment/email/operations callback secrets or URLs. The existing runtime validator requires an AI key setting even for read-only recovery. Use an explicitly invalid, non-secret sentinel with the installed SDK's base URL pinned to loopback port9; any model request must fail locally. Never claim AI availability from this configuration. Use the existing non-production `qa` runtime classification because `recovery` is not accepted by the frozen runtime; this does not target or change the QA app. A reserved invalid owner email prevents accidentally granting a real recovery user owner status.

Ingress permits only the operator IPv4 /32, with one replica maximum. Use normal image startup and Google managed authentication; no fabricated session/principal. Prove denied access from the previously established QA vantage and correct authenticated isolation before inviting sign-in. Google client `557534798515-f1419k6iqqb0ptk6b1a4f8qs20uqd1hv.apps.googleusercontent.com` currently has seven callbacks. Exactly one proposed addition is `https://filosage-recovery-app-20260912.salmontree-eb10220f.centralus.azurecontainerapps.io/.auth/login/google/callback`; preserve all existing entries. Browser-tool action-time confirmation for that addition is pending. This is a specific authentication change, not a new release policy.

Cleanup deletes only resources created and owned by this run, after evidence retention; preserve shared network/DNS zone/storage/registry/vault, all original Blob objects, production/QA apps and database owners. Remove the exact temporary Google callback after the rehearsal through the approved cleanup scope. Verify original production health/configuration and QA presence afterward. Cleanup command success alone does not prove soft-delete retention configuration.
