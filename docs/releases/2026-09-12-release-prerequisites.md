# September 12 release prerequisite readiness

Owner: release-ops worker, isolated `codex/release-ops-20260912`, starting `40f02c7`. Coordinator owns the complete R1–R9 outcome and `docs/AGENT_PROGRESS.md`. This package is read-only provider investigation plus reviewed local tooling; it does not authorize or execute a deployment, provider change, secret read, or rollback.

## Package checklist and acceptance

- [x] Inventory exact routing, authentication, image identity, runtime secret references, environment protections and backup inputs using bounded metadata reads.
- [x] Compare the actual predecessor's startup/health and account/publication/accounting protocol against the release contract.
- [x] Supply a repeatable, redacted prerequisite check and verify failure cases locally.
- [x] Specify ordered, resource-scoped operations and evidence required to clear provider gates, including a compatible predecessor transition.
- [x] Prepare validated source/tooling/report for the local checkpoint; independent code/probe/helper and maintenance-packet reviews completed. Actual commit SHA is returned in the coordinator handoff.

Risks: a zero-traffic revision remains reachable and shares durable services; legacy in-flight writes ignore newer fences; changing shared identity/secret/auth state affects both revisions; seven-day backup retention does not prove a restore. No fixture, metadata read, or worker review substitutes for hosted proof or Victor's explicit approval of a concrete production operation recorded in the task/handoff. Keep checkout closed, preserve migration-dual and current scale. Actual mutations and their exact review belong to the coordinator.

Implementation plan: add a read-only metadata collector with fail-closed assessment; exercise missing/malformed environment protections, absent backup inputs, legacy health and broad runtime grants; retain only bounded derived output. Use existing release contracts without relaxation. Produce the compatible transition and privilege cutover procedure from observed source/provider evidence. No helper will mutate Azure or GitHub.

## Actual production evidence

Read-only observations on September 12, including the coordinator-reviewed database probe at 15:16:31 UTC:

| Boundary | Observed result | Consequence |
| --- | --- | --- |
| App | `filosagestg-app`, resource group `filosage-staging-central-rg`; Multiple mode; `green-93f60f24-1` 100%, `blue-7abae96f-1` 0%; both active/Healthy | Preserve exact revision bindings. A zero-weight old writer is still active. |
| Public identity | HTTP 200, SHA `93f60f24afe59b19b6a592f455a09e8e813f1f84`, digest `sha256:0c006852322a91d5e2540cfd27ab58e47dad33a3a1f793fd6afc1e2240bbadf2` from revision metadata | Legacy health lacks `imageDigest` and `capabilities`; the runtime also lacks the complete manifest. Existing preflight correctly cannot accept it. |
| Shared auth | Easy Auth/HTTPS, Google and External ID enabled; `migration-dual`; hash `9bf95fcfbfd064ecbbae1fe88db80f071572ddbc772d714d99da5c9e7ba1aee6` | Keep shared provider settings and canonical origin unchanged. |
| Scale | 0.5 CPU / 1 GiB; min 0 / max 3 | No scaling change performed. |
| Database runtime | `filosageadmin`, eight direct memberships; effective DML, schema/database CREATE, elevated membership and table-owner membership all true | Runtime fails minimum-access startup requirements. A separate non-owner login is needed. |
| Ownership | Database `filosage` owner `filosageqa_app`; document table owner `filosageadmin`; schema owner `azure_pg_admin` | Preserve these shared owners. The QA-named database owner must not be dropped/revoked during QA retirement. |
| Schema/ACL | Required eight columns and primary key compatible; PUBLIC database/schema CREATE false | No schema migration, ownership transfer or PUBLIC revoke is justified by this evidence. |
| Counts/settings | 510 documents, 13 courses; two database/runtime sessions including the probe; server max connections 50, idle-session and idle-transaction timeouts 0 | Point-in-time counts, not customer content. No runtime pool override; exact legacy source defaults to 10 connections per process. |
| Query plan | Read-only EXPLAIN of public course collection equality, limit 20: collection-path bitmap index, estimated 13 index rows; heap/limit cost 18.34, estimated one result | An index is already used. This is a representative estimate without execution timings, ordering or `ANALYZE`, not a production performance claim. |

[Sanitized database evidence](2026-09-12-production-database-inventory.json) identifies the exact revision, image and probe-source hash. The transaction rolled back. No application DML, secret-value extraction, model call, or restore was performed. Both active revision environments reference the same five application secrets; shared authentication references two more. Neither active revision includes bootstrap administrator/password environment names. The old provisioner therefore skips role provisioning, but its default Docker startup still runs the schema migration through `DATABASE_URL`.

The inventory uncovered a source defect independent of the legacy gate: Azure CLI returns display location `Central US`, while the release state requires `centralus`. `azureLocationName` now normalizes validated provider display names before snapshot validation; invalid punctuation, leading/repeated spaces and newlines still fail. No health, manifest, writer or human-review requirement was relaxed.

## GitHub and recovery gates

Initial reads found only `azure-staging` and `production-operations`, each with main-only branch policy and no reviewer rule. The repository is private. Repository-level variables/secrets are empty; the staging environment supplies deployment variables and the three OIDC identity secrets. Under Victor's superseding September 12 policy, stage, candidate review and promotion all use existing `azure-staging`, with exactly one allowed deployment branch rule (`main`, type `branch`) and its existing OIDC identity/trust. Verify the actual issuer/audience/subject and required input names; no new environment or federated subject is required.

Historical evidence under the superseded policy: the coordinator's attempted native required-reviewer environment creation returned HTTP 422 because the billing plan does not support that protection. A subsequent worker read detected a partially created, unprotected `azure-candidate-review` with admin bypass and no branch restriction. The coordinator removed that empty environment and confirmed only the original environments remain. Do not assume a rejected provider write is atomic.

Victor subsequently approved removing native required reviewers, self-review prevention and administrator-bypass prevention as release prerequisites. His explicit approval of the concrete production operation must be recorded in the task/handoff before execution. This procedural control is not GitHub-enforced independent review; `reviewedBy`, dispatch and environment metadata do not prove approval. Preserve hosted packet validation and honest review attribution. No Enterprise, ownership transfer, new collaborator, distinct initiator, new environment or machine approval protocol is required. The earlier native-gate decision and organization/reviewer question are superseded; [current approval policy](2026-09-12-native-deployment-gate.md) governs.

Backup observation was initially blocked by two absent inputs and then by the deployment principal's missing ARM server-read permission. The coordinator supplied the approved exact server ID/retention 7 and added built-in Reader only at that Flexible Server, preserving other assignments. Assignment `1ac35c24-3844-40d7-8c13-926a631f82fa` belongs to deployment principal `6a25b4c2-f60a-4995-82ae-d2ea64786577`. This grants ARM server/child metadata reads, no PostgreSQL login/data/DDL permission. [Run 34701167484, attempt 2](https://github.com/Victor-CS-Core/Filosage/actions/runs/34701167484/attempts/2) succeeded; artifact `10299594406`, digest `sha256:338d6c368365a5048d49ad85bc78a1e354fe35b2e234563f3f4abb4a4c83e2ff`, was downloaded by the coordinator. Worker direct read found Ready, retention 7, earliest restore point September 6 at 00:07:39.641251 UTC. This closes observation configuration/access, not a current restore rehearsal.

Before release, produce a current approved PostgreSQL/Blob recovery rehearsal with a private isolated restore target, time/consistency checks, retained records and cleanup evidence. Do not manufacture a restore proof from retention settings or the successful observation artifact. Existing subscriber inventory is coordinator-owned; the coordinator reports the live subscription list is empty, which must be retained with the candidate billing-containment evidence.

## Minimum-access correction plan

The production runtime principal is `ed1a4afb-922a-4a2f-a70e-37602cad5dbf`. Its observed vault-wide Secrets User assignment is `767eec1e-c898-5743-ae0c-90c1f8ed4d5a` at vault `filosagestg-p4ujucgnxq3g`. Registry pull and course-banner Blob grants are separate and must be preserved.

An independently scoped IAM containment step can precede the database/bridge transition. Fresh active-revision and shared-auth metadata proves the exact required Key Vault secret names are `activity-receipt-secret`, `database-url`, `identity-link-hmac-secret`, `migrated-owner-uid`, `openai-api-key`, `google-easy-auth-client-secret`, and `external-id-client-secret`. The union accounts for all seven current app-secret references. Current source has no direct Key Vault client use outside those bindings.

1. Capture app/revision/auth/secret-reference hashes and all effective role assignments. Re-read exact target identity, active revisions and consumers immediately before any mutation.
2. Idempotently add Secrets User only at each of those seven secret scopes, preserving versioned app references, database credentials and unrelated grants. Reject conflicting/conditional assignments or unexpected consumers.
3. Run a status-only actual managed-identity required-access probe from the selected runtime: all seven must return 200 without retaining response bodies.
4. Remove only the captured vault-wide assignment after preconditions still match. Repeat seven successful reads and require 403 for administrator and QA-exclusive secret names; re-read effective grants, auth/routing and health. If the result is uncertain, retain exact evidence and recover the captured assignment only under the reviewed operation's rollback plan.

This leaves the existing `database-url` administrator credential accessible intentionally until the database cutover. It proves narrower vault scope, not a non-admin database runtime. Removing that secret now or changing the old runtime login would break or endanger legacy startup. Worker did not execute IAM changes.

For database minimum access, create/select a distinct non-owner production login (proposed `filosage_runtime`) after checking that exact name and its existing grants. Give only `CONNECT` on `filosage`, `USAGE` on its `public` schema and SELECT/INSERT/UPDATE/DELETE on `public.filosage_documents`; negative role attributes, no memberships, no ownership, no grant options. Preserve all existing owner roles/ACLs. Do not run the broad bootstrap target, overwrite an existing password, rotate `database-url`, transfer ownership, or change PUBLIC ACLs. Create a separately versioned runtime-secret reference and bind it only to modern revisions. Require actual TLS login, full catalog allow/deny evidence, rolled-back unique-row DML, current `verify-azure-database.ts`, default startup marker and exact health before accepting it. The operator's secure credential channel and concrete role/secret operation still need review; this package does not create credentials.

After modern predecessor/candidate acceptance and legacy drain, remove the obsolete app-secret reference and only this runtime identity's scope at `database-url`. Retain the underlying secret/owner roles for separately reviewed shared consumers and recovery. Re-probe required modern reads and administrator/QA denial; neither an IAM snapshot nor a successful startup proves the other boundary.

## Compatible predecessor and first transition

Legacy `93f60f24` lacks `account-lifecycle.ts`, `account-write-fence.ts`, `publication-proofs.ts`, `generation-operations.ts` and the guarded lesson commit. Current document-store integration adds account-generation locks, publication proof and epoch checks, immutable attempts, original-period settlement and atomic lesson/results/accounting. Those are independent of traffic weights and feature flags. The [writer cutover contract](2026-09-lesson-writer-cutover.md) explicitly disallows assuming compatibility from the new binary's feature cohorts.

A health-only backport or startup override would not repair the legacy write protocol. A complete legacy backport would pull in the account/publication/accounting dependencies and the relevant document-store/API changes, then need the same independent review, engineering and mixed-image PostgreSQL proof. The inspected six core files alone differ by over 1,000 added lines. No minimal safe backport is established.

Recommended path is the already contemplated separately reviewed first-transition initialization, using a complete modern compatible baseline and a bounded maintenance/write-quiescence window:

1. Freeze a modern baseline B containing every current account/publication/accounting fence and the read-only startup, with exact SHA/digest, full tests and isolated PostgreSQL old/new compatibility/rejection evidence. Prefer the complete integrated protocol set over an unproved backport. Define final candidate C separately; even if the app source is identical, retain distinct exact revision identities. Do not label either image a verified predecessor before its actual hosted proof.
2. Prepare B's immutable image, manifest and distinct-runtime-login evidence without creating, starting or accessing a modern application revision. Shared ingress/auth and public revision URLs provide no proven per-revision isolation here. Defer revision creation, secret binding, startup and hosted inspection until the all-origin fence and complete old-writer drain below pass. Preserve shared auth/origin/flags.
3. Obtain actual human review of the exact initialization packet and a concrete maintenance enforcement mechanism. The old binary has no universal fence switch: ordinary feature flags cannot establish quiescence. Block new ingress to **all old revision and label origins**, plus external jobs/maintenance and callbacks able to write; account/session GET paths may write too. Preserve auth configuration. A shared ingress access restriction or an upstream maintenance control must be evaluated for its exact reach; DNS or 0% weight alone is insufficient.
4. Drain old requests/jobs/leases with a stated 15-minute bound and final reads. Record pending requests, publication epochs, attempt status and accounting settlement without exposing records. Preserve uncertain provider costs and reconciliation obligations. Deactivate the old revisions only after reviewed drain handling; prove their replica/process and old database sessions are gone. If sessions cannot be attributed or terminated safely, stop and retain maintenance. Do not terminate a shared administrator/owner's sessions indiscriminately.
5. Only after complete old-writer drain, create B at zero public weight under maintenance using the reviewed initialization operation. Re-inventory the new origin and prove denied/allowed access, then verify B's new login, read-only startup, full health/manifest/digest/auth contract and hosted account/publication/accounting tests. Establish the verified B as the live baseline with a distinct modern compatible zero-weight revision. There is no automatic writable fallback to either legacy image after modern writes begin. Any exceptional old-code restoration requires continued write denial plus separately verified state recovery.
6. Once B is proven, normal staging preflight can capture it as the predecessor. Run the unchanged staging/review/promotion contracts for C, retain the successful exact-SHA quality/full-regression and seven immutable hosted proof artifacts, and retain packet review attribution and obtain Victor's explicit approval for the concrete production operation. Promote without rebuilding C; use only B for compatible fallback. Open public access only after the reviewed maintenance exit gate and actual learner/monitoring evidence.

This is a concrete operational proposal, not an executed maintenance window or a replacement for existing hosted gates. Victor selected preparation of the maintenance path and subsequently removed the native-review prerequisite while retaining explicit approval of concrete production operations. The [maintenance packet](2026-09-12-maintenance-transition.md) records exact coverage, remaining inputs, drain/recovery/exit rules and read-only preflight. Compatible-baseline, current restore and hosted overlap proofs remain outstanding; native reviewer capability is no longer a release blocker. Never stage with fabricated predecessor health or set `rollbackAuthorized` for the legacy images.

## Tooling and verification

Repeatable metadata check (read-only, exit 1 for failed metadata prerequisites):

```sh
node scripts/check-release-prerequisites.mjs Victor-CS-Core/Filosage filosage-staging-central-rg filosagestg-app migration-dual
```

It emits bounded metadata/derived booleans only, never variable values, secret bodies or provider errors. It always leaves `releaseReady: false`: successful metadata cannot certify a restore, database/secret access probe, Victor's actual operation approval, or hosted writer compatibility. The historical live check successfully inventoried all boundaries and failed closed for the then-current legacy, broad-vault and missing-reviewer conditions; the missing-reviewer prerequisite is now superseded. Backup input presence became true after the coordinator's correction.

The separate `run-production-database-inventory.py` pins target SHA/digest/revision and the reviewed transport hash, verifies a secret reference without extracting it, uploads only public probe source, removes the temporary source, and has a 120-second process bound. It imports transport definitions only; the historical one-shot QA bootstrap is guarded by `if __name__ == "__main__"` and was not run. One actual execution was reviewed and completed. Its SQL uses BEGIN READ ONLY, bounded statements/locks, EXPLAIN without ANALYZE and finally ROLLBACK; there are no DML/DDL/ACL statements. Subsequent independent review identified libpq URL query options overriding validated host/TLS. The fixed helper rejects unapproved/duplicate URL options and constructs `pg.Client` using explicit verified connection fields, never a connection string. Offline regressions and independent re-review passed. The retained live evidence identifies the earlier probe source hash; no additional live DB execution occurred after this hardening.

`contain-production-vault-access.py` and `production-vault-status-probe.mjs` form the separately reviewed exact-seven containment package. Plan mode produces a fingerprint of current configuration, required references and effective assignments in a private metadata directory. Apply requires that unchanged reviewed fingerprint and its explicit execution flag; it grants only exact scopes with deterministic IDs, preserves every unrelated assignment, checks seven allowed versioned reads, re-reads before removing the one captured broad assignment, checks seven allowed/eight existing administrator-or-QA denied names, and compares final configuration/assignments. Source hashes, before/intent/after/stopped evidence and the 600-second whole-operation bound make retries reviewable. Secret bodies are canceled without reading; redirects fail. Its scope intentionally retains `database-url`. Execution/status is coordinator-owned; this worker prepared and offline-tested the helper and did not run it.

The coordinator executed that reviewed containment. Its first run passed all seven required reads before removal, removed the captured broad assignment, and stopped at the post-removal allow/deny check without retaining a status matrix. Fresh provider readback found zero broad grants, exactly seven narrow grants and unchanged configuration; public health remained 200 on the old SHA. No outage or exact failure cause was established. The worker added a separate `--probe-read-only` mode and closed-schema diagnostic retention, without adding mutations. After review, the coordinator's read-only diagnostic passed all seven required 200 responses and all eight administrator/QA 403 responses, with unchanged snapshots. This closes the observed vault-scope containment gate; PostgreSQL administrator runtime and modern startup/compatible-predecessor gates remain open. No broad-grant rollback was needed.

Local verification is recorded at handoff. The first broader release-contract run produced 75 passes and one expected suite-inventory count failure because the new registered contract suite increased the count from 39 to 40. That count is corrected while exact discovered-suite ownership checks remain intact. These checks do not claim whole-application regression, provider corrections by this worker, a deployed source fix, current recovery or release completion.

Final local checkpoint verification: 81/81 release, blue-green, capability, bootstrap-isolation and hardening contracts passed in 13.9 seconds. ESLint, TypeScript no-emit, Python compilation and whitespace checks passed. The secret scanner flagged the newly added synthetic DSN fixture; its explicit fake-credential marker was corrected using the existing scanner exception mechanism, then the focused URL-override test and unchanged scanner passed. Owned `.browser-discovery-*` / `.matrix-fixture-*` / `.matrix-list-fixture-*` test trees were absent after the test finalizers; no unrelated workspace or retained evidence was deleted. Independent review accepted the URL hardening, prerequisite assessment/collector, normalization and database transport; the coordinator reviewed the IAM diagnostic and maintenance packet. Package source is ready for local commit and coordinator integration. Push, integrated CI, modern deployment, maintenance execution, current restore and production acceptance remain separate outcomes.
