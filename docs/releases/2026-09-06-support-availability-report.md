# R16 support availability and documentation reconciliation

The selected candidate keeps Command Center off. A public no-store capability route now checks both the environment flag and the normalized persisted intake control. It exposes only submission/history booleans; unavailable datastore evidence returns 503. The browser offers composition only after a valid available result. Loading, off and unknown states offer published email and Help; existing authenticated ticket history and public replies remain readable. The Support page does not advertise an owner queue or a submission form while intake is unavailable.

Authenticated list, detail and submit requests use the shared account-generation request wrapper and browser epoch checks. Account changes clear private views and drafts; late responses cannot populate another session. Unchanged retries retain their idempotency key, double submission remains guarded, and a failed request preserves text, including a copyable view if intake turns off.

## Executed verification

- Actual support capability, submit, list, detail, owner-triage and public-reply route functions executed against an isolated real local datastore. Concurrent unchanged submissions returned one durable ticket; a different learner received 404; the published reply appeared in requester readback while the internal owner note did not. Environment-off and owner-control-off both rejected POST while history remained readable. No provider calls were made.
- Seven focused process tests passed: the new route/store contract and six existing support-wiki documentation contracts. The temporary Playwright configuration ran process/APIRequestContext checks only; this is not browser acceptance.
- The support wiki checker passed all 19 articles. TypeScript, focused Oxlint and ESLint passed. Four new browser cases cover off, unknown, failed-message preservation after intake closes, and late private response after session invalidation. They are authored but unexecuted locally because the available browser directory contains no browser executable. Root owns integrated browser CI and shared suite-manifest registration for `tests/support-availability.spec.ts` and the expanded support-center suite.

## Wiki and owner handbook impact

Genuine guidance was reconciled in follow-a-course, complete-a-capstone, manage-and-publish-a-course, report-content, create-a-course, plans-and-billing, read-evidence-report, complete-a-lesson, accessibility, use-review, manage-profile, getting-started, sign-in-help, privacy-controls and contact-support. Topics include account-scoped learning, durable generation recovery, exact validation and immutable publication, conditional support/report intake, approved Portal transitions, and truthful deletion/retention status. The owner handbook now distinguishes the one-ContainerApp blue/green candidate from hosted acceptance, removes obsolete QA workflow sources, describes current intake-off behavior and preserves billing, privacy, legacy-client and monitoring acceptance gates. Its selected owner context remains a reviewed draft aid; no new owner permission or automatic action was added.

No mailbox monitoring, mail delivery, independent alert acknowledgment, hosted billing acceptance, legal retention duration, deployment, traffic swap or external wiki publication was claimed or performed. The source article catalog and owner handbook are committed for the normal application/wiki release path. Source implementation and local verification are complete; integration, browser/hosted acceptance and actual support coverage remain root/owner gates. Multica is unavailable; no tracking ID was invented. Push and provider operations belong to root.
