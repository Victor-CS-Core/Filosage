# September 2026 operator handoff and evidence packet

Prepared September 6, 2026 from source baseline `f01cc15a5fc9fb83e59398551c5a46e7e1a540a2` plus the monitoring changes accompanying this document. **Status: prepared, not accepted.** This is an unfilled execution and review packet for R19, R24 and R26 and their dependencies. No hosted observation, recipient acknowledgment, professional review, provider configuration, restore, promotion or commercial activation was performed to prepare it. Source tests do not close those gates. R01–R26 release acceptance remains blocked until the corresponding actual evidence and independent decisions are retained.

Victor is the accountable release owner pending explicit assignment. “Release owner,” “operator,” “independent reviewer,” “support owner” and “qualified reviewer” below are roles, not claims that another named person accepted work. Do not fill a signature or acceptance on someone else's behalf. This packet records operational evidence requirements; it does not establish a legal entity, jurisdiction or legal conclusion.

## Current instructions and historical records

Use [single-application BFF operations](../BLUE_GREEN_BFF_OPERATIONS.md), [production operations](../PRODUCTION_OPERATIONS.md), [recovery rehearsal](../RECOVERY_REHEARSAL.md) and the committed [capability manifest](../../config/release-capabilities.json). This packet supersedes separate-QA/Sites deployment or QA-equals-production instructions in the older [commercial launch runbook](../COMMERCIAL_LAUNCH_RUNBOOK.md). Historical August observations, checked boxes, owner policy decisions and old provider object lists remain dated history; they are not September readbacks.

The release unit is **one Azure Container App, one Next.js frontend plus same-origin `/api` BFF image, and two revision labels**. The inactive candidate starts at zero public traffic. It is still reachable and shares durable services with the active revision; it is not an isolated database or permission to inject production failures. Use approved accounts and data. Real dependency-loss drills require a separately approved isolated target or an explicitly approved, bounded live incident exercise. Readiness/liveness evidence must distinguish these methods.

Freeze an exact full SHA, immutable image digest, manifest and authentication fingerprint. Obtain exact-SHA engineering and full-regression evidence, stage the immutable candidate with authorization, collect the seven hosted gate artifacts, independently review, then request the specific traffic-swap approval. Promotion reuses the reviewed image. It does not rebuild it or change shared Easy Auth configuration. Preserve exact 100/0 bindings and the reviewed compatible predecessor; zero traffic does not stop in-flight requests or bypass account, publication and accounting fences.

The current manifest closes optional features and learner rollout (`pipelineOwnerOnly: true`, `pipelineCohortPercent: 0`). It does not authorize widening them. `BILLING_ENABLED=false` keeps new commercial entry closed pending Gate B and explicit activation; existing-customer lifecycle processing, cancellation/recovery and webhook monitoring must remain operational under that closure. The former instruction that Plus has one active owned course is superseded by the accepted credit policy: Plus two monthly credits/cap 24, Pro five/cap 60, with purchased/generated course access preserved according to current entitlement rules. Verify the exact hosted behavior before commercial acceptance.

## Execution identity and decisions — all unfilled

Use restricted evidence storage for account-linked records. Public/GitHub artifacts contain sanitized IDs, timestamps, summaries and non-secret object/version references, not claims, tokens, DSNs, email addresses, private prompts, learner work or raw provider responses. Each result needs its method, exact scope, UTC observation time, operator, independent reviewer and immutable evidence location/digest. A proposed procedure cannot be labeled `passed`.

| Required field | Value / current state |
| --- | --- |
| Accountable release owner | Victor; execution responsibilities and availability not yet accepted |
| Named executing operator / fallback operator | UNFILLED — owner to assign and obtain acknowledgment |
| Independent technical reviewer | UNFILLED — owner to assign; no reviewer acceptance recorded |
| Support, payment and qualified legal/privacy reviewers | UNFILLED — roles to assign independently |
| Approved operation and UTC window / maximum duration | UNFILLED — deployment, tests, sends, restoration and promotion each require their actual authorized scope |
| Candidate full SHA / image digest / manifest fingerprint | UNFILLED — baseline above is not a deployed candidate identity |
| Exact app resource ID / candidate revision / inactive label URL | UNFILLED — provider readback required |
| Active revision / image / full SHA / blue and green weights | UNFILLED — exact 100/0 binding readback required |
| Canonical origin / shared auth mode and fingerprint | UNFILLED — compare candidate and active configuration |
| Engineering / full-regression run IDs and attempts | UNFILLED — successful exact-candidate runs and artifacts required |
| Candidate stage / verification / promotion run IDs | UNFILLED — no run is attested by this packet |
| Hosted gate artifact identity | UNFILLED — successful run, workflow, artifact name and SHA-256 digest, underlying transcripts |
| Protected environment reviewers / exclusive deployment lease | UNFILLED — provider protection readback and named operator acknowledgment |
| Minimum safe rollback SHA / exact predecessor revision and digest | UNFILLED — overlapping-reader/writer, schema and fence compatibility review required |
| Incident thresholds / escalation deadlines / recovery targets | UNFILLED — Victor must approve measurable values before unattended operation |

The seven exact `scripts/blue-green-review.ts` gate names are `bff-auth-privacy`, `learner-journey`, `publication`, `billing-containment`, `operations-probes-alerts`, `recovery-rollback` and `data-write-compatibility`. Bind every artifact to the exact candidate as specified in the BFF runbook. This Markdown packet is not a substitute for `candidate-proof.json` or a valid signed-off workflow review packet. Leave `result`, `reviewedBy` and approval fields unfilled until the actual review occurs.

## R19: monitoring, receiver and probe evidence

Source behavior reviewed: startup and liveness use dependency-free handlers. Readiness checks configuration and bounds the datastore check at 3,500 ms; Bicep gives the readiness probe a 5-second timeout. Liveness is not a database-availability signal. The fuller `/api/health` endpoint reports release SHA/digest/capabilities/origin/auth mode and datastore availability; the external monitor must impose its own request deadline and compare these fields with the frozen candidate.

The alert sender signs the exact JSON body with HMAC-SHA256 and sends a version, alert ID, timestamp and idempotency key. Retries preserve the same envelope, are bounded, and retry transient errors. Source fixes in this handoff restore backoff when `Retry-After` is absent and label a 2xx response **HTTP delivery acceptance only**. The admin overview also treats legacy sender `succeeded` records as transport-only, so these cannot establish receiver readiness. No receiver implementation, independent uptime service, durable sender queue or acknowledgment/escalation channel is provided by these sender contracts. Exhausted delivery logs need an independently observed response path.

| ID / accountable role | Exact evidence to collect after authorization | Current result / missing evidence |
| --- | --- | --- |
| M1 / release owner → operator | Current app/revision probe configuration, timing/threshold readback, cold-start timestamps and revision events. Show startup and liveness pass when the process serves requests, readiness remains unavailable until configuration/DB are ready. | BLOCKED — no hosted probe/configuration/event artifacts |
| M2 / operator + independent reviewer | Approved dependency-loss method and target; timestamps and request results showing readiness 503 within its bound, liveness 200, recovery to ready, restart counts before/after, and no DB-induced liveness restart loop. Retain isolation and restoration evidence. | BLOCKED — no authorized hosted fault drill; zero-traffic shared services are insufficient isolation |
| M3 / monitoring owner | Independent monitor ID/location, `/api/health` target, configured 1–5 minute cadence, request deadline, exact SHA/digest/origin/auth/capability comparison, two-consecutive-failure trigger and recovery rule. Retain failed responses, notification timestamps, recovery response and cleared-incident event. | BLOCKED — monitor and delivery readbacks unfilled |
| M4 / receiver owner | Receiver deployment/resource ID, code/configuration version, independent hosting/failure domain, secret reference version only, timestamp tolerance, exact-body HMAC verification and durable storage/retention. Establish when HTTP acknowledgment occurs relative to durable commit. | BLOCKED — receiver identity, implementation review and storage evidence absent |
| M5 / operator + receiver owner | With separately approved send scope, retain signed test ID, source SHA, HTTP result, receiver durable record ID/commit time and readback after receiver restart. Prove valid retry with the same ID produces one durable event; test altered body, invalid signature and stale timestamp are rejected without paging/acceptance. | BLOCKED — no messages sent; source HMAC/retry tests and a 2xx response cannot establish these outcomes |
| M6 / independent monitoring owner | Independently observed receiver outage/failed delivery plus receiver recovery. Correlate sender exhausted attempts and receiver availability monitor; prove failure of Filosage or its DB cannot silently disable the only alert path. | BLOCKED — independent receiver monitor and actual outage/recovery evidence absent |
| M7 / on-call operator + fallback | Real alert delivery to the approved channel, human acknowledgment timestamp and identity, approved acknowledgment deadline; exercise missing acknowledgment escalation to the assigned fallback and recovery/closure. Retain provider event IDs and sanitized timeline. | BLOCKED — primary/fallback assignments, delivery, acknowledgment and escalation unfilled |
| M8 / release owner | Actual Azure Monitor rules/action groups, revision failure/restart thresholds, PostgreSQL storage/connections, Blob errors, Easy Auth failures, AI failures/rate limits/cost ceilings, and Stripe webhook/credit reconciliation failure monitoring even while new checkout is closed. Name each responding owner and runbook. | BLOCKED — current resource/rule IDs, thresholds, receiver readbacks and owners unfilled |
| M9 / operator + reviewer | Post-swap bounded signed-in smoke and independent uptime/receiver observations correlated to promotion time and exact revision. Record any error-rate, generation recovery or billing anomalies and the decision to hold or recover. | BLOCKED — no promotion/post-promotion observations |

`npm run test:operations-alert -- --evidence-file=<approved-new-private-path>` is a **real send**, not an offline contract test. It can also write `operationalEvidence/alert-test-latest` when a database URL is configured. Review the exact receiver and database targets and obtain explicit send/write authorization before execution. Its output now says `status: transport_accepted`, `receiverDurableAcknowledgement: unverified` and `independentReceiverMonitoring: unverified`. Store independent receiver evidence separately and have it reviewed; do not edit sender records to manufacture a healthy dashboard. Offline `test:contracts` with injected fetch implementations does not contact the receiver.

## R18 dependency: measured RPO/RTO and rollback

Follow [RECOVERY_REHEARSAL.md](../RECOVERY_REHEARSAL.md) for exact resource scope, supported networking, separate PostgreSQL and Blob destinations, disabled outbound providers/workers, current-schema/role and ownership/reference checks, measurement and approved cleanup. A backup-window observation explicitly has `restoreRehearsal: false`; an old seven-day-retention screenshot cannot satisfy this table.

| ID / accountable role | Required evidence | Current result / missing evidence |
| --- | --- | --- |
| D1 / Victor | Maximum tolerable committed-work loss (RPO), validated-service recovery time (RTO), availability target and budget/lifetime ceiling, with dated approval. | UNFILLED — no values or approval asserted |
| D2 / operator | Exact source DB/Blob resource IDs; current retention/window/versioning/soft-delete readbacks; OIDC principal effective inherited read permissions; manual and next scheduled backup-window result and failure owner. | BLOCKED — current provider evidence absent |
| D3 / operator + independent reviewer | Approved recovery point and isolated destinations, actual restore operation IDs, network/private access and privilege proof, start/ready/connect/validated timestamps, current schema and account/generation/deletion/evidence/billing references, restored Blob version/checksum/ownership consistency. | BLOCKED — no rehearsal performed |
| D4 / reviewer | Known committed-marker gap and measured elapsed recovery compared with D1; unresolved inconsistencies; exact cleanup authorization/readback or approved bounded retention. | BLOCKED — RPO/RTO unmeasured; no reviewer decision |
| D5 / operator + reviewer | Actual compatible predecessor image/SHA and schema/fence tests, in-flight write overlap evidence, exact traffic rollback procedure and readbacks, fresh signed-in validation. Keep data recovery separate from revision traffic rollback. | BLOCKED — no hosted rollback drill or compatibility acceptance |

## R24: commercial, legal and service-delivery evidence

Keep new billing entry closed until all applicable Gate B evidence and explicit activation approval exist. Current source policy does not establish Stripe live objects, tax registration, legal compliance, a support mailbox or a functioning notice provider. Do not use historical August object descriptions as current configuration. Ask the assigned qualified reviewer to evaluate the exact operator identity, applicable jurisdictions, hosted wording and customer journey; do not infer a business identity or professional approval from this packet.

| ID / accountable role | Exact evidence to collect | Current result / missing evidence |
| --- | --- | --- |
| C1 / payment operator + reviewer | For **Plus monthly, Plus annual, Pro monthly, Pro annual**, record test and live account/mode, Product and Price IDs, currency/amount/interval, active state, tax behavior and effective configuration version. Compare all four with exact candidate pricing/checkout; retain historical allowed Price IDs and unknown-price fail-safe results. | BLOCKED — four current live/test Price inventories and review unfilled |
| C2 / payment operator | Exact Portal configuration ID/version and enabled controls/return URL; webhook endpoint ID, mode/URL, subscribed events and signing-secret version reference; signed test delivery/event IDs and retry/duplicate/out-of-order results. Ensure closed new billing does not disable existing-customer webhook reconciliation/cancellation/recovery. | BLOCKED — provider readbacks and hosted signed lifecycle matrix absent |
| C3 / payment operator + qualified reviewer | Current registrations and territory decision, Stripe Tax configuration/behavior, accepted payment methods, statement descriptor, receipt/invoice settings, refund policy and actual refund handling. Resolve each discrepancy against approved terms before checkout opens. | BLOCKED — current commercial/provider facts and qualified decisions absent |
| C4 / independent qualified reviewer | Version/hash of hosted terms, privacy, age/paid eligibility, renewal/cancellation/refund, retention/deletion, source/copyright/AI and analytics notices; exact operator/contact identity and applicability reviewed; dated findings, disposition and reviewer acceptance. Preserve approved August 25 immediate plan/interval changes, `always_invoice`, unchanged anchor and ordinary cancellation at period end unless separately approved. | BLOCKED — independent review, identity confirmation and hosted comparison unfilled |
| C5 / support owner + backup | Actual support and legal mailbox destinations, routing/ownership/access controls, approved inbound/outbound test message IDs, received timestamps, human acknowledgment and fallback/escalation. Historical mailbox statements do not establish current delivery. | BLOCKED — owner assignment and mailbox round-trip evidence absent; no message sent |
| C6 / notice provider owner | Provider/account/domain and non-secret configuration references; transactional template IDs/versions, trigger-to-delivery correlation, delivered/rejected/bounced/suppressed events, retry/escalation owner, consent/marketing opt-out behavior and appropriate transactional routing reviewed. Use approved test recipients only. | BLOCKED — provider configuration, authorized send and delivery/bounce/suppression evidence absent |
| C7 / support + release owner | Open privacy, security, account/deletion, entitlement, refund and unsafe-content incidents; severity/impact, evidence, containment, accountable owner, deadline and independent closure decision. No unresolved high-risk incident before commercial activation. | BLOCKED — current incident inventory and closure review absent |
| C8 / Victor | Exact reviewed candidate, Gate B artifact references, named operator/reviewer decisions, activation scope/cohort, spend guard, support coverage and rollback trigger; separate explicit activation approval. | UNFILLED — no commercial activation authorized or claimed |

## Hosted auth, privacy and flagship dependencies

| ID / accountable role | Required evidence | Current result / missing evidence |
| --- | --- | --- |
| H1 / operator + independent reviewer | Real Google/Easy Auth sign-in, legitimate migrated-account linking and logout/recovery on approved accounts; server header-forgery rejection, verified-account/owner isolation, recent-auth destructive control and shared-auth readback at the exact candidate. No raw claims or identities in public artifacts. | BLOCKED — local route tests cannot prove hosted identity ownership |
| H2 / privacy reviewer + operator | Anonymous structure-only access and two-account isolation for lesson bodies, notes, progress, quizzes, evidence shares and account deletion; concurrent write/delete and publication visibility behavior, private asset cleanup/references and retained legal records. Retain sanitized cross-account denial results. | BLOCKED — actual hosted privacy/data-write-compatibility artifacts absent |
| H3 / learning reviewer + release owner | Exact named flagship course/version and all lesson IDs; complete real-provider generation/usage evidence, human pedagogical/source/language/assessment review, publication proof and signed-in learner journey. R13 offline fixtures or a first-lesson success are not a completed flagship. | BLOCKED — flagship identity, complete real-provider output and independent acceptance unfilled; paid generation not authorized by this packet |
| H4 / release owner | Reconcile all R01–R26 items with exact current source/CI, hosted/provider/human evidence and unresolved findings; preserve R19/R24/R26 blockers and dependency gates explicitly. | BLOCKED — this document does not mark any overall release gate complete |

## R26: bounded observation, support and next decision

Before the authorized swap, Victor must fill the observation start/end, polling cadence, measurable stop thresholds, operator availability and fallback acknowledgment deadline. Collect a baseline immediately before the swap, observations at the approved cadence, and a final status at the deadline. Do not leave an unbounded watcher or treat a lack of reported complaints as evidence of successful service.

| Observation / responsible role | Record and action | Current handoff state |
| --- | --- | --- |
| Availability/authentication / on-call | Error rates and request counts by revision/window, latency, fresh signed-in result, external monitor and receiver state. At approved threshold, stop expansion and apply only the specifically approved containment/compatible rollback; retain incident and readback IDs. | UNFILLED — time window, baseline, thresholds, assigned operator and observations |
| Generation/credit durability / operations owner | Started/completed/failed/pending/unknown operations and denominators, recovery age/resume outcome, duplicate charge or missing credit, actual/uncertain provider costs and ceilings. Reconcile uncertain outcomes before retrying paid work; retain operation IDs privately. | UNFILLED — cohort/window, ceilings and measured outcomes |
| Existing billing / payment owner | Webhook backlog/retries, unknown Prices, subscription/credit reconciliation and cancellation/recovery outcomes regardless of checkout closure. Do not replay uncertain live payment effects without investigation and explicit authority. | UNFILLED — queue baselines, incidents and named owner |
| Support and notices / support owner | Intake volume/category/response and escalation time, delivered versus bounced/suppressed notices, entitlement/deletion/refund/privacy cases and owner actions. Request only necessary verification; never passwords, OTPs or full card details. | UNFILLED — coverage, actual delivery and incident evidence |
| Nonblocking issue queue / release owner | Each issue gets impact, containment, named assignee, deadline, evidence link and the condition that reopens a launch blocker. Unassigned or disputed severity is unresolved. | UNFILLED — current issue inventory and assignee acknowledgment |
| Weekly review / Victor | Use the **already-created Monday review automation**; record its existing reference when available, accountable reviewer and each decision artifact. Review retention/completion, recovery, cost, safety/privacy reports, refunds and support with real denominators/windows. Do not create a duplicate automation. | Existing automation creation reported by release owner; reference/owner acknowledgment and future review outcomes UNFILLED |
| Expansion decision / Victor + reviewers | Keep optional features, additional public courses and learner cohort closed until exact feature/course evidence, complete generation cost/quality, recovery and support capacity meet separately approved criteria. Any manifest/cohort change needs a new scoped decision and candidate verification. | BLOCKED — no expansion evidence or authorization |

At the final observation deadline record: actual UTC end; candidate still serving and traffic readback; metric window/denominators; incidents and remaining risks; continue/hold/approved rollback decision; next review trigger; operator and independent reviewer acknowledgment. All are **UNFILLED**. The next safe step is to assign these roles and prepare the exact candidate/resource/test-recipient inventory for review. Only then request the bounded hosted operations and any necessary real sends. This document grants none of those permissions.
