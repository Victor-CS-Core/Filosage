# September12 production runtime preparation

Victor approved fixing the next blocker: distinct production runtime login and safe connection budget. This packet applies the already recorded minimum-access design. Coordinator owns exact provider operations and evidence; independent review precedes mutations. R1–R9 remains active. Existing owner/database credentials and production traffic are preserved.

Subscription bfc8f890-2681-43dc-8eac-51644341ae12; group filosage-staging-central-rg; PostgreSQL filosagestg-p4ujucgnxq3gs-pg; database filosage; source transport only exact active filosagestg-app--green-93f60f24-1 at93f60f24afe59b19b6a592f455a09e8e813f1f84 and digestsha256:0c006852322a91d5e2540cfd27ab58e47dad33a3a1f793fd6afc1e2240bbadf2. Private TLS-verified explicit PG options reject DSN overrides. The existing admin credential remains inside that container.

Create only an absent login filosage_runtime, LOGIN/NOSUPERUSER/NOCREATEDB/NOCREATEROLE/NOINHERIT/NOREPLICATION/NOBYPASSRLS, CONNECTION LIMIT18. Give direct CONNECT on filosage, public USAGE and SELECT/INSERT/UPDATE/DELETE on public.filosage_documents. No ALTER existing password, broad bootstrap, PUBLIC revoke, ownership transfer, other role/member grants or production-content edits. PostgreSQL may grant the creating administrator incoming membership in the new role; allow only that known creator, while the runtime itself must inherit/be granted no role. A pre-existing role is verification-only with the retained private password; incompatible credentials/privileges stop without repair or rotation.

Fresh preflight must verify databaseownerfilosageqa_app, schemaownerazure_pg_admin and tableownerfilosageadmin, no document-table user triggers/RLS and PUBLIC CREATE denial. Provision transaction uses the existing bootstrap advisory lock and rechecks role absence. Verify all four privileges individually, no grant options/extra direct ACLs/elevated memberships/ownership/QA CONNECT, rolecap18, TLS runtime login and a unique releaseRuntimeProbes row's INSERT/SELECT/UPDATE/DELETE inside a rolled-back transaction. Verify row absence afterward and execute the current verify-azure-database.ts schema SQL, retaining source hash. Capacity proof is separately retained: server50 minus reserved5/superuser10 leaves35; modern2revisions×3replicas×(2main+1health)=18, QA1×(10+1)=11, operations6. Current legacy10pool/oldsecret remain until complete maintenance/drain; this preparation does not fix live legacy pool/configuration by itself.

New secret only: database-url-runtime-v1 in vaultfilosagestg-p4ujucgnxq3g, with a distinct immutable version after successful role verification. Generate/retain one256bit random runtime password in task-owned ignored0600 file, send only over reviewed echo-disabled transport, and construct only the new dedicated DSN locally. Save via private file with CLI logs/telemetry disabled; retain no secret values or passwords in outputs/Git. If the named secret already exists, compare privately and never overwrite an unrelated value. If an uncertain write may have succeeded, read it back before retry and retain the same credential. After verified vault persistence, remove local secret/password files. No old secret is changed or removed.

Grant existing production managed identity only Key Vault Secrets User at that exact new secret scope, with a deterministic assignment identity and fresh principal/resource checks; preserve all existing scoped assignments. Verify actual production-identity read succeeds and QA cannot read it. No shared vault-wide grant. Do not bind the new secret to the legacy app, start modern revisions, or remove the old credential grant in this step. New runtime binding, startup marker/health and removal of old-admin access happen only at the separately reviewed modern transition.

Each private probe has a180second local/150second remote limit. Secret/IAM execution and readback has a ten-minute deadline with bounded reads. On failure retain the newly created dedicated role/credential securely for idempotent recovery; never delete a possibly consumed role or rotate credentials to hide uncertainty. No deployment is part of this packet. Record provider/login/source-validation evidence, preserve failed attempts, independently review results, compare source config/auth/traffic/health, commit/push coherent checkpoints and state remaining transition gates.

## Verified preparation result

The dedicated role and18connectioncap passed actual TLS/catalog/rolled-backDML/schema verification. The separate versioned credential was saved and compared privately; production MI reads it and QA receives403. Existing configuration/auth/traffic and unrelated grants are unchanged. Temporary local password material was removed after verification. See [role evidence](../research/artifacts/release-readiness-20260912/runtime-apply-20260912.json), [secret evidence](../research/artifacts/release-readiness-20260912/runtime-secret-persistence-20260912.json) and [cleanup](../research/artifacts/release-readiness-20260912/runtime-secret-cleanup-20260912.json).

[PR27](https://github.com/Victor-CS-Core/Filosage/pull/27) contains the reviewed source pool/capacity enforcement. It is not deployed. The legacy app remains on its original credential and pool configuration. Modern revision binding, startup/concurrency acceptance and later removal of administrator access remain part of the separately reviewed maintenance transition.
