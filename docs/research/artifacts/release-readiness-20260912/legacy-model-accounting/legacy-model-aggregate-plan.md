# Legacy production model accounting aggregate proposal

Prepared September 12 at 17:42 UTC for coordinator review. **Not executed.** No provider/database call, code change, new dependency or paid model request occurred in this investigation. Current release candidate 3a0e772 is unchanged.

## Source findings and interpretation

Inspected deployed legacy source `93f60f24afe59b19b6a592f455a09e8e813f1f84` through Git. `src/lib/ai-usage.ts:176–182,300–307` stores mutable `aiRequests` request snapshots and monthly `usagePeriods`, `userAiBudgets` and `systemUsageShards` summaries. Finalization (`:373–441,443–529`) persists request token totals, `actualCostMicros`, model names and usage-sample array, and increments the same finalized cost in all three summary collections. These are not independent receipts and must not be added together.

`aiRequests` is keyed by the hashed idempotency request. Retrying a failed/stale reservation replaces its snapshot and reservation timestamps, while cumulative budget cost remains. Retention/account deletion can also remove request records. Therefore current snapshots may underrepresent historical attempts; count them as surviving snapshots, not all paid requests. Per-sample counts are recorded usage samples, not proven provider calls: default zero-usage samples and fixed-cost search/image samples also exist. The SQL deliberately returns no request, account, response, result, cache or prompt identifier, no prompt/profile text and no receipt JSON.

`src/lib/ai-pricing.ts:21–74` calculates stored cost from then-configured rates, a fallback rate or a fixed microdollar amount. `generate-course/route.ts` includes fixed-cost samples at lines294/341/531 and image cost at1138. Report sums as **recorded application estimates**, not invoice-verified spend or current rates. Preserve integer microdollars (1,000,000 per USD); do not reprice. Cached/cache-write input tokens are subsets of input tokens, not additional input.

The request's `createdAt` is reservation time and `updatedAt` is finalize/retry update time. The sample array has no start/end/duration field. Their difference includes queueing, application work, multiple calls and retry behavior; it cannot measure model-call latency. No latency percentile will be derived. Separate pipeline observability supports stage durations and persists `coursePipelineEvents` as well as console output; those events are not part of the accounting-sample schema. Stage durations are not automatically model-call durations and are outside this usage/cost-only packet. A full source search at93 finds `durationMs` only in that optional interface, with no emitted assignment at its route call sites. The coordinator's seven-day console-log absence does not imply no model usage.

## Exact query packet

See adjacent `legacy-model-aggregate-plan.sql`. Execute the four aggregate queries only after the identity/read-only guard passes. A repeatable-read transaction makes all views share one snapshot. Each statement has a15-second limit, locks3seconds, idle transaction30seconds, client connection15seconds/query20seconds, and one outer120-second operation deadline. Disable parallel workers for this read. Abort if either allowed collection exceeds10,000documents or any attempts array exceeds64; never present truncated totals as complete. At most28request groups,5model groups,4budget groups and one inventory row may leave SQL. NULL sums remain unavailable; invalid/missing numeric/timestamp counters identify incomplete coverage. Empty groups establish no retained records, not zero provider spend. Reserved totals represent unsettled reservations, which may include in-flight paid work, and must never be described as certainly unspent.

Query2 reports all surviving request snapshots grouped only by six fixed feature names plus `other`, and three fixed statuses plus `other`. Query3 reports the finalized snapshots' nested samples grouped by four source-default model labels plus `other`. Query4 reports September2026 cumulative system-shard estimates by three fixed budget pools plus `other`. This month is a fixed proposed parameter, not private input. Query2/3 have no asserted seven-day scope; the row timestamp ranges describe their actual retained coverage. Query4 is a monthly budget view and cannot answer a seven-day spend question. Never sum across these three views or add `reservedCostMicros` to finalized spend. `usagePeriods`/`userAiBudgets` are not queried.

## Reviewed transport reuse and pre-execution gate

The existing transport source `/home/ktr0nn/Work/Filosage/.worktrees/visitor-resume/.superpowers/sdd/outcome-checklist/qa-bootstrap/qa_bootstrap.py` still hashes to `ebf0b21413caea1127f81cfc398547b0460d4c2bef73b77e972e595fd2de1f40`. Its bootstrap main is under the `__name__ == '__main__'` guard. Import definitions only and use `PtyTransport`; never invoke bootstrap, vault/role functions or its original main. The already reviewed `scripts/run-production-database-inventory.py` supplies exact app/revision/image/env-ref validation and bounded private PTY framing, and `scripts/inspect-runtime-database.mjs:72–83` supplies hardened explicit pg options with verified TLS and URL-option containment. Preserve these guards.

Before any database connection, read-only ARM metadata must confirm app `filosagestg-app`, group `filosage-staging-central-rg`, subscription `bfc8f890-2681-43dc-8eac-51644341ae12`, exact active revision `filosagestg-app--green-93f60f24-1`, full legacy SITE_VERSION above, image `filosagestp4ujucgnxq3gsacr.azurecr.io/filosage@sha256:0c006852322a91d5e2540cfd27ab58e47dad33a3a1f793fd6afc1e2240bbadf2`, one container and secret-reference DATABASE_URL. No environment value is printed. Inside that revision, use only its existing runtime DATABASE_URL and strict existing host/database/5432/TLS validation. SQL independently requires database `filosage`, currently observed legacy login `filosageadmin` and READ ONLY; abort on drift.

The old inventory wrapper cannot directly run this query packet unchanged: it hardcodes its catalog collector and operation schema. A **new private aggregate adapter and closed output validator must be prepared and reviewed before execution**; no adapter was executed or silently substituted here. Import only the existing definitions, preserve hard source hashes, send source (no secrets) under umask077, verify transmitted source length/hash, frame only validated aggregate JSON, and clean its temporary source in an EXIT/HUP/INT/TERM trap plus bounded transport cleanup. Never log connection options, SQL errors, transport/provider output, or arbitrary result keys. Return only a generic failure code on any exception. Output validation must reject extra keys, unknown bucket labels, invalid timestamp ranges, noninteger/negative/unsafe totals, oversized result sets and target mismatch before any serialization. Failed validation is no financial evidence.

Required offline review tests for that adapter: exact expected queries and fixed month parameter, BEGIN READ ONLY/ROLLBACK even on error, strict pg URL/TLS options before connection, closed aggregate key/type/row-count validation, missing numeric totals remain null, bucket poisoning maps to `other`, oversized attempt arrays fail before query3; missing/non-array attempts stay counted as incomplete coverage, and duplicate-view costs never combine. No new dependency is needed. Coordinator must approve exact adapter/SQL hashes and exact target before any new provider DB probe.

The resulting aggregate, if later approved and successful, is a **legacy production database snapshot**, which may contain retained records produced by earlier revisions too. It is not exact per-revision accounting or candidate3a0e772 proof. No source SHA is stored per usage sample, and the shared DB must not be described as exclusively generated by the executing green revision.

No owned background process was started; all local Git/file/hash inspection commands exited. This proposal intentionally stops before any provider/DB execution.
